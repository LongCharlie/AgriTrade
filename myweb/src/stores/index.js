//Create an instance of the store
import { createStore } from 'vuex'
import tab from './tab'

export default createStore({
    state: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
        tab //induct tab
    }
})

